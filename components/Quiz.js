import React, { Component } from 'react';
import { View, Text } from 'react-native'

class Quiz extends Component {
  render() {
    return (
      <View style={{flex: 1}}>
        <Text>Add Card</Text>
      </View>
    );
  }
}

export default Quiz